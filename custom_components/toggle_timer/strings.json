{
  "config": {
    "step": {
      "user": {
        "title": "Toggle Timer",
        "description": "Set up the backend services and timer state entity.",
        "data": {}
      }
    },
    "abort": {
      "single_instance_allowed": "Only one Toggle Timer integration can be configured."
    }
  }
}
